class CliOptions:

    def __init__(self, username, context, network, limit, online):
        self.username = username
        self.context = context
        self.network = network
        self.limit = limit
        self.online = online